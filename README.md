# health-insurance-sg-server

## Setup

You might need to first install `uv` via `pip install uv`.

Then run the following:

```
$ uv sync
$ source .venv/bin/activate
$ python3 -m uvicorn main:app
```
